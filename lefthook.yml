pre-commit:
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: TURBO_UI=0 pnpm biome:check --log-order=stream -- --no-errors-on-unmatched --files-ignore-unknown=true && git update-index --again
